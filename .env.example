# CATS (Crawl and Test System) Configuration
# Copy this file to .env and modify values as needed

# =============================================================================
# DIRECTORY CONFIGURATION
# =============================================================================

# Root directory for the application (default: current working directory)
# CATS_ROOT_DIR=/path/to/cats

# Public directory for generated files (default: public/)
# CATS_PUBLIC_DIR=/path/to/cats/public

# Source directory for templates and assets (default: src/)
# CATS_SRC_DIR=/path/to/cats/src

# Reports directory for generated accessibility reports (default: public/reports/)
# CATS_REPORTS_DIR=/path/to/cats/public/reports

# Legacy reports directory for backwards compatibility (default: same as CATS_REPORTS_DIR)
# CATS_LEGACY_REPORTS_DIR=/path/to/cats/legacy/reports

# Templates directory (default: src/templates/)
# CATS_TEMPLATES_DIR=/path/to/cats/src/templates

# Source styles directory (default: src/styles/)
# CATS_STYLES_DIR=/path/to/cats/src/styles

# Source scripts directory (default: src/scripts/)
# CATS_SCRIPTS_DIR=/path/to/cats/src/scripts

# Public styles directory (default: public/styles/)
# CATS_PUBLIC_STYLES_DIR=/path/to/cats/public/styles

# Public scripts directory (default: public/scripts/)
# CATS_PUBLIC_SCRIPTS_DIR=/path/to/cats/public/scripts

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3000)
# CATS_SERVER_PORT=3000

# Server host (default: localhost)
# CATS_SERVER_HOST=localhost

# =============================================================================
# CRAWLER CONFIGURATION
# =============================================================================

# Maximum number of pages to crawl (default: 25)
# CATS_MAX_PAGES=25

# Default WCAG version to test (default: 2.1)
# Options: 2.0, 2.1, 2.2
# CATS_WCAG_VERSION=2.1

# Default WCAG compliance level (default: AA)
# Options: A, AA, AAA
# CATS_WCAG_LEVEL=AA

# Job concurrency settings
# Maximum number of concurrent crawl jobs (default: 3)
# CATS_MAX_CONCURRENT_JOBS=3

# Default number of parallel browsers per crawl job (default: 4)
# CATS_DEFAULT_CRAWLER_CONCURRENCY=4

# Time to keep completed jobs in memory before cleanup (default: 300000ms = 5 minutes)
# CATS_JOB_CLEANUP_DELAY_MS=300000

# Maximum runtime for a single job before timeout (default: 3600000ms = 1 hour)
# CATS_MAX_JOB_RUNTIME_MS=3600000

# =============================================================================
# APPLICATION BRANDING
# =============================================================================

# Application short name (default: CATS)
# CATS_APP_NAME=CATS

# Application full name (default: CATS (Crawl and Test System))
# CATS_APP_FULL_NAME=CATS (Crawl and Test System)

# Application description (default: Automated accessibility testing and reporting system)
# CATS_APP_DESCRIPTION=Automated accessibility testing and reporting system

# =============================================================================
# GOOGLE CLOUD STORAGE (PRODUCTION)
# =============================================================================

# Enable cloud storage for production deployment (default: false)
# CATS_USE_CLOUD_STORAGE=true

# Google Cloud Project ID
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud Storage bucket name for reports
# CATS_STORAGE_BUCKET=your-cats-reports-bucket

# =============================================================================
# CLOUD RUN DEPLOYMENT
# =============================================================================

# Server host - use 0.0.0.0 for Cloud Run (default: localhost)
# CATS_SERVER_HOST=0.0.0.0

# PORT is automatically set by Cloud Run - don't override in production
# PORT=8080

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Set to 'development' to enable debug logging and config output
# NODE_ENV=development

# Base URL for local development report links
# BASE_URL=http://localhost:3000

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Example: Custom installation in different directory
# CATS_ROOT_DIR=/opt/accessibility-testing
# CATS_REPORTS_DIR=/var/www/html/accessibility-reports
# CATS_SERVER_PORT=8080

# Example: High-volume testing configuration
# CATS_MAX_PAGES=100
# CATS_SERVER_PORT=3001

# Example: Custom branding for organization
# CATS_APP_NAME=AccessibilityBot
# CATS_APP_FULL_NAME=AccessibilityBot - Enterprise Edition
# CATS_APP_DESCRIPTION=Enterprise accessibility testing and compliance reporting

# Example: Different WCAG standards
# CATS_WCAG_VERSION=2.2
# CATS_WCAG_LEVEL=AAA

# =============================================================================
# GOOGLE CLOUD SERVICE ACCOUNT CREDENTIALS
# =============================================================================

# IMPORTANT: Service account key file setup
# 1. Download your service account key from Google Cloud Console
# 2. Save it as 'service-account-key.json' in the project root
# 3. Add to .gitignore (already included)
# 4. Set environment variable below:

# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# Alternative: Use gcloud auth for local development
# gcloud auth login
# gcloud auth application-default login

# =============================================================================
# DOCKER DEVELOPMENT NOTES
# =============================================================================

# For Docker development:
# - Local storage: Files saved to ./public/reports/ (mounted volume)
# - Cloud storage: Set CATS_USE_CLOUD_STORAGE=true in docker-compose.yml
# - Port mapping: Container port 3000 maps to localhost:3000

# For Google Cloud Run deployment:
# - Environment variables set automatically by cloudbuild.yaml
# - PORT variable set by Cloud Run (don't override)
# - Service account authentication handled by GCP